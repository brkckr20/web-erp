BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[malzeme] ADD [derece] VARCHAR(100), [ozellik_3] VARCHAR(200), [ozellik_4] VARCHAR(200);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
